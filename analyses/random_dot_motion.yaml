analysis_id: analysis.random-dot-motion.baseline-plan
protocol_id: protocol.random-dot-motion-classic-macaque
dataset_id: dataset.roitman-shadlen-rdm-pyddm
status: real-file-static-report-run
inputs:
  - processed Roitman-Shadlen CSV downloaded from pinned PyDDM commit
verified_outputs:
  - 6149 canonical trial rows across two macaques
  - 11 psychometric summary rows by signed coherence
  - 6 chronometric summary rows by absolute coherence
  - descriptive analysis result JSON with fitted psychometric estimates
  - dependency-free SVG psychometric plot
  - dependency-free SVG chronometric plot
  - static HTML report
grouping_variables:
  - signed target-coded motion coherence
  - absolute motion coherence
  - monkey
implemented_outputs:
  - canonical trial table
  - psychometric summary by signed target-coded coherence
  - chronometric summary by absolute coherence
  - fitted four-parameter logistic psychometric estimates
  - static report over source provenance, psychometric rows, chronometric rows, and artifact links
deferred_outputs:
  - drift-diffusion model fit
  - per-monkey psychometric and chronometric split
  - explicit target-position mapping from original MATLAB files
provenance_requirements:
  - PyDDM CSV URL and commit hash
  - source file hash
  - adapter version or commit hash
  - date generated
  - target-coding caveat
