# Implementations

This directory will hold metadata records for task-control code and reference implementations.

The atlas distinguishes:

- **Used implementation**: code actually used to produce a dataset.
- **Reference implementation**: reusable code that approximates a protocol for teaching, testing, or reuse.

