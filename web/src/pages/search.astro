---
import BaseLayout from "../layouts/BaseLayout.astro";
import SearchPanel from "../components/SearchPanel.svelte";
import PageHeader from "../components/ui/PageHeader.astro";
import { searchIndex } from "../lib/search";

const total = searchIndex.counts.total;
---

<BaseLayout
  title="Search · behavtaskatlas"
  description="Cross-record search over papers, protocols, datasets, slices, findings, models, stories, data requests, and comparisons."
>
  <PageHeader
    title="Search"
    lede={`Search across ${total} records — papers, task families, protocols, datasets, vertical slices, findings, models, stories, data requests, and curated comparisons.`}
  />

  <SearchPanel client:load />
</BaseLayout>
