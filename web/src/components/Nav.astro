---
const primary = [
  { href: "/findings", label: "Findings" },
  { href: "/papers", label: "Papers" },
  { href: "/models", label: "Models" },
  { href: "/slices", label: "Slices" },
  { href: "/compare", label: "Compare" },
];

// Secondary nav stays on the page for first-time orientation: stories
// (editorial), catalog (full directory), network (relationship graph
// across the four record types). Atlas-health is operational state for
// contributors and lives in the footer instead.
const secondary = [
  { href: "/stories", label: "Stories" },
  { href: "/catalog", label: "Catalog" },
  { href: "/graph", label: "Network" },
];

const currentPath = Astro.url.pathname.replace(/\/+$/, "") || "/";

function isActive(href: string): boolean {
  if (href === "/") return currentPath === "/";
  return currentPath === href || currentPath.startsWith(`${href}/`);
}
---

<nav class="border-b border-rule bg-surface-raised">
  <div class="mx-auto max-w-5xl px-4 py-3">
    <div class="flex items-center gap-3">
      <a
        href="/"
        class="shrink-0 font-semibold tracking-tight text-fg no-underline"
        aria-label="behavtaskatlas home"
      >
        behavtaskatlas
      </a>

      <form action="/search" method="get" class="relative ml-auto min-w-0 flex-1 sm:max-w-48">
        <label class="sr-only" for="nav-search">Search the atlas</label>
        <input
          id="nav-search"
          type="search"
          name="q"
          placeholder="Search…"
          class="w-full rounded-md border border-rule-strong bg-surface-raised px-2 py-1 pr-9 text-body-xs focus:border-rule-emphasis focus:outline-none focus:ring-1 focus:ring-accent"
          autocomplete="off"
        />
        <kbd
          class="pointer-events-none absolute right-1.5 top-1/2 hidden -translate-y-1/2 rounded bg-surface-sunken px-1 py-0.5 font-mono text-mono-id text-fg-muted sm:inline-block"
          aria-hidden="true"
        >⌘K</kbd>
      </form>
    </div>

    <div class="mt-2 -mx-4 overflow-x-auto px-4">
      <div class="flex w-max items-baseline gap-x-5">
        <ul class="flex items-center gap-x-5 text-body">
          {
            primary.map((item) => (
              <li>
                <a
                  href={item.href}
                  aria-current={isActive(item.href) ? "page" : undefined}
                  class:list={[
                    "no-underline transition-colors whitespace-nowrap",
                    isActive(item.href)
                      ? "border-b-2 border-accent pb-0.5 text-fg font-medium"
                      : "text-fg-secondary hover:text-accent",
                  ]}
                >
                  {item.label}
                </a>
              </li>
            ))
          }
        </ul>

        <ul class="flex items-center gap-x-3 text-body-xs text-fg-muted">
          {
            secondary.map((item) => (
              <li>
                <a
                  href={item.href}
                  aria-current={isActive(item.href) ? "page" : undefined}
                  class:list={[
                    "no-underline whitespace-nowrap",
                    isActive(item.href)
                      ? "text-fg font-medium"
                      : "hover:text-accent",
                  ]}
                >
                  {item.label}
                </a>
              </li>
            ))
          }
        </ul>
      </div>
    </div>
  </div>
</nav>
