# OS / editor
.DS_Store
*.swp
*~
.idea/
.vscode/

# secrets — checked-in templates allowed via !.env.example
.env
.env.*
!.env.example

# logs
*.log

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Project-local data + generated artifacts (see CLAUDE.md)
data/raw/
derived/

# JS deps and build artifacts (any depth)
node_modules/
dist/
.astro/
*.tsbuildinfo

# Stray top-level JS — web/ has its own package.json + bun.lock and those
# should be tracked; the repo root is Python-only.
/package.json
/bun.lock
/package-lock.json
/yarn.lock

# Vercel CLI local state
.vercel/
